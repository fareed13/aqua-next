'use client'

import type { SectionProps } from '@/components/sections/registry'

export function ReferralHero(_props: SectionProps) {
  return (
    <div
      className="relative pb-[25%] pt-[130px]"
      id="maitre-widget"
    />
  )
}
